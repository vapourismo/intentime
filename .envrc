use flake

# Nix injects SDK vars that point to an old Apple SDK incompatible with the
# host Xcode Swift compiler. Unset them so `swift build` uses the Xcode SDK.
unset SDKROOT DEVELOPER_DIR NIX_CFLAGS_COMPILE NIX_LDFLAGS MACOSX_DEPLOYMENT_TARGET
